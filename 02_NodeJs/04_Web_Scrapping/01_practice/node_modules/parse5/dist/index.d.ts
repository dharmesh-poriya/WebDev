import { type ParserOptions } from 'parse5/dist/parser';
import type { DefaultTreeAdapterMap } from 'parse5/dist/tree-adapters/default';
import type { TreeAdapterTypeMap } from 'parse5/dist/tree-adapters/interface';
export { type DefaultTreeAdapterMap, defaultTreeAdapter } from 'parse5/dist/tree-adapters/default';
export type { TreeAdapter, TreeAdapterTypeMap } from 'parse5/dist/tree-adapters/interface';
export { type ParserOptions, /** @internal */ Parser } from 'parse5/dist/parser';
export { serialize, serializeOuter, type SerializerOptions } from 'parse5/dist/serializer';
export type { ParserError } from 'parse5/dist/common/error-codes';
/** @internal */
export * as foreignContent from 'parse5/dist/common/foreign-content';
/** @internal */
export * as html from 'parse5/dist/common/html';
/** @internal */
export * as Token from 'parse5/dist/common/token';
/** @internal */
export { Tokenizer, type TokenizerOptions, TokenizerMode, type TokenHandler } from 'parse5/dist/tokenizer';
/**
 * Parses an HTML string.
 *
 * @param html Input HTML string.
 * @param options Parsing options.
 * @returns Document
 *
 * @example
 *
 * ```js
 * const parse5 = require('parse5');
 *
 * const document = parse5.parse('<!DOCTYPE html><html><head></head><body>Hi there!</body></html>');
 *
 * console.log(document.childNodes[1].tagName); //> 'html'
 *```
 */
export declare function parse<T extends TreeAdapterTypeMap = DefaultTreeAdapterMap>(html: string, options?: ParserOptions<T>): T['document'];
/**
 * Parses an HTML fragment.
 *
 * @example
 *
 * ```js
 * const parse5 = require('parse5');
 *
 * const documentFragment = parse5.parseFragment('<table></table>');
 *
 * console.log(documentFragment.childNodes[0].tagName); //> 'table'
 *
 * // Parses the html fragment in the context of the parsed <table> element.
 * const trFragment = parser.parseFragment(documentFragment.childNodes[0], '<tr><td>Shake it, baby</td></tr>');
 *
 * console.log(trFragment.childNodes[0].childNodes[0].tagName); //> 'td'
 * ```
 *
 * @param fragmentContext Parsing context element. If specified, given fragment will be parsed as if it was set to the context element's `innerHTML` property.
 * @param html Input HTML fragment string.
 * @param options Parsing options.
 * @returns DocumentFragment
 */
export declare function parseFragment<T extends TreeAdapterTypeMap = DefaultTreeAdapterMap>(fragmentContext: T['parentNode'] | null, html: string, options: ParserOptions<T>): T['documentFragment'];
export declare function parseFragment<T extends TreeAdapterTypeMap = DefaultTreeAdapterMap>(html: string, options?: ParserOptions<T>): T['documentFragment'];
//# sourceMappingURL=index.d.ts.map